import { DashboardLayout } from "@/components";

const Settings = () => {
  return (
    <DashboardLayout title="Settings">
      <div className="flex items-center justify-center">Settings</div>
    </DashboardLayout>
  );
};

export default Settings;
